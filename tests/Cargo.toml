[package]
name = "tests"
version = "0.0.0"
edition = "2021"
publish = false

[features]
alloc = ["bin-proto/alloc"]

[dependencies]
bin-proto = { path = "../bin-proto", default-features = false, features = ["derive", "prepend-tags"] }
